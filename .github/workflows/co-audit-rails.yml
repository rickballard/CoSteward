name: co-audit-rails (seed)
on:
  pull_request:
  workflow_dispatch:
jobs:
  rails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: placeholder
        run: echo "co-audit rails seed (replace with real logic)"