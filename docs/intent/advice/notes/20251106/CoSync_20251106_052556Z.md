# CoSync — guard gating verified (20251106_052556Z)

**Repo:** rickballard/CoSteward

**Merged**
- Job-level gate added; push removed. Guard now runs only on **pull_request** + **workflow_dispatch**.

**Validated**
- Dispatch on \$Ref\ shows **event=workflow_dispatch** and **jobs>0** with guard log lines.

**Next**
- Keep PRs **hard-fail**; dispatch = **warn-only** (sticky report). Continue journaling guard runs.
